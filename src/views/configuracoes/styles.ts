import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  campos: {
    marginTop: 20,
    gap: 20,
    flexDirection: "column",
    alignItems: "center",
  },
  alterarSenha: {
    alignItems: "center",
    marginTop: 20,
  },
});
