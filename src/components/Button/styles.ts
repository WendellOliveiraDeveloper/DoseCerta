import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#73adde",
    alignItems: "center",
    padding: 10,
    borderRadius: 10,
    alignSelf: "auto",
  },
  title: {
    color: "#ffffff",
    fontSize: 16,
    fontWeight: "400",
  },
});
